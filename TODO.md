# TODO

- [ ] Fix Telegram submission in `completeinfo.html` by ensuring `js/config.js` is loaded and the Telegram bot/chat IDs are available.
- [ ] Add a guard in `submitForm()` to alert if botToken/chatId are missing.
- [ ] Verify by submitting the form and confirming Telegram message delivery (or at least no runtime errors in console).

